# Backend Notes

This first version uses Laravel controllers with demo arrays instead of database queries. Later commits can add:

- Eloquent models
- Form requests
- Services for judging and scoring
- Role middleware
- Queue jobs for submissions
- Admin approval workflow
